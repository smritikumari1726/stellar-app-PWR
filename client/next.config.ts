import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['svc-2202b3b4-morphvm-fvb2h4gy.http.cloud.morph.so']
};

export default nextConfig;
